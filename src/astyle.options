style=stroustrup    # bracketing style
--break-closing-brackets
break-blocks        # Pad empty lines around header blocks (e.g. 'if', 'for', 'while'...).
--pad-oper
--pad-header
indent=spaces=4
--convert-tabs      # Converts tabs into spaces in the non-indentation part of the line.

#removed this for now (not sure what is preferred for the book)
#--add-brackets      # Add brackets to unbracketed one line conditional statements 